

## Plan: Capture Product Shots from Main Branch

Now that you're on `main` with your real bakesale data, I'll capture three key screens and turn them into polished macOS-framed product shots.

### Screenshots to Capture

1. **Landing / Passcode screen** — the first thing visitors see after scanning the QR code
2. **Main bakesale page** — recipe info, personal message, donation options (after entering passcode `appleton2026`)
3. **Admin wizard** — the setup flow (via `/admin?pw=bakesaleadmin`)

### Process

1. Use the browser to navigate to the preview, take screenshots of each screen
2. Run each through the product shot generator with a gradient preset that complements the warm cookie theme (likely `peach` or `candy`)
3. Save all three to `/mnt/documents/` for download

### After Screenshots

Once you've downloaded the product shots, I'll:
- Switch context to sanitize `main` (same changes already done on `template`)
- Update the README with image references
- You make the repo public on GitHub

